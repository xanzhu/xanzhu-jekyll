url: "https://xanzhu.com"
baseurl: "" 
title: "xanzhu"

exclude: 
  - Gemfile
  - Gemfile.lock
  - yarn.lock
  - vendor
  - node_modules
  - _site
  - CNAME
  - package.json
  - scripts
  - _All-Snippets

include:
  - _pages

sass:
  style: compressed
  sass_dir: _sass

plugins:
  - jekyll-sitemap

# Let users know whats happening with the site 
# Supports 503 (Maintenance), 
code: 503